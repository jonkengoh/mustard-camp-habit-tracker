{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Habit Tracker",
            "type": "debugpy",
            "request": "launch",
            "module": "discord_habit_tracker.main",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}
